# -*- mode: snippet -*-
# name: changelog
# key: changelog
# --
* `(format-time-string "%a %b %d %Y")` `(string-trim (shell-command-to-string "git config --global user.name"))` <`(string-trim (shell-command-to-string "git config --global user.email"))`> - `(my-changelog-version-release)`
- $0
- Resolves: RHBZ#$1
