# -*- mode: snippet -*-
# name: changelog
# key: changelog
# --
* `(format-time-string "%a %b %d %Y")` `(string-trim (shell-command-to-string "git config user.name"))` <`(string-trim (shell-command-to-string "git config user.email"))`> - `(my-changelog-version-release)`
- $0
- Resolves: RHBZ#$1
